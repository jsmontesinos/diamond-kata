import diamond from '../src/diamond';

describe('diamond kata', () => {
  
  // Act before assertions
  beforeAll(async () => {
    
  });;

  // Assert greeter result
  it('If the input is null, return empty diamond', () => {
    
  });

});
